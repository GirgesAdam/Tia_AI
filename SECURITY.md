# Security Policy

Tia AI handles operational clinic data and should be treated as a security-sensitive application.

Do not open public issues for vulnerabilities. Report them privately to the repository owner/security contact.

Never commit `.env`, `.env.local`, Supabase secret keys, Gemini/API credentials, production database credentials, patient exports, database dumps, access tokens, cookies, or webhook secrets. Use synthetic data in tests and examples.
